## 2025-12-13

**做了什么**
1. 建立了自己的GitHub库。
2. 做了第一个python项目。
3. 学习了python基本语法，基本数据类型，数据类型转换。

**收获**
1. 想要获得一个[0,1)区间的随机数，可以利用random.random()这个函数。
拓展如下：<br>
[0, N) 的浮点数	r * N	               random.random() * 10	          生成 [0.0, 10.0) 的随机小数<br>
[A, B) 的浮点数	A + r * (B - A)	       5 + random.random() * (10-5)	  生成 [5.0, 10.0) 的随机小数<br>
[A, B] 的整数	random.randint(A, B)   random.randint(1, 6)	          生成 1 到 6 的随机整数（含两端）<br>
7. 字符串格式化方法f-string，利用{}放置一块动态区域。
8. :.4f保留四位小数。
9. 需要随机数，请调用random库。

**明日计划**
1. 学习python教程。
2. 获取新项目，锻炼逻辑与技巧。


## 2025-12-15

**做了什么**
1. 练习了运算符，定义字典，二进制转换等等操作。

**收获**
1. 字典的数据类型。 
quantum_terms 前者是键，后者是值。 只需要记住键的名字，就可以调用值。<br>
.items()	 取出所有键和值（成对的元组）	     把字典的每一页（词条）完整复印下来。
.keys()	     只取出所有键	                   只复印字典的索引页（所有单词列表）。
.values()	 只取出所有值	                   只复印字典里所有的解释内容。
.get(“键”)   安全地取出某个键对应的值	        查字典，如果找不到该词，不会报错，而是返回默认值。

2. bin()是系统内置的二进制转换器。
3. 通过int()，可以转换为整型，如果需要转换二进制数需要标注2。

**感想**
我想写很多，未免太过做作。只是这几日确实算不上努力，引以为耻，戒骄戒躁。
keep moving gogogo
